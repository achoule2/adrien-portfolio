---
const navLinks = [
  { name: 'Adrien', href: '/' },
  { name: 'Compétences', href: '/Competences' },
  { name: 'Portfolio', href: '/portfolio' },
  { name: 'Témoignages', href: '/témoignage' },
];
---

<header class="flex items-center justify-between px-6 md:px-12 py-4 bg-white shadow-sm relative">
  <!-- Logo / Nom -->
  <h1 class="text-2xl md:text-3xl font-bold text-green-600 tracking-tight">
    Adrien Choulet
  </h1>

  <!-- Navigation Desktop -->
  <nav class="hidden md:flex space-x-8 text-gray-800 font-medium">
    {navLinks.map(link => (
      <a href={link.href} class="hover:text-green-600 transition-colors duration-200">
        {link.name}
      </a>
    ))}
  </nav>

  <!-- Bouton CV -->
  <a href="/image-209.png" class="hidden md:inline-block border-2 border-green-600 text-green-600 px-5 py-2 rounded-lg hover:bg-green-50 transition text-sm font-semibold">
    📄 Télécharger CV
  </a>

  <!-- Menu Mobile -->
  <button id="menu-btn" class="md:hidden text-green-600 focus:outline-none">
    ☰
  </button>

  <!-- Décoratif : points colorés -->
  <div class="absolute inset-0 pointer-events-none z-0">
    <div class="absolute top-4 left-4 w-2 h-2 bg-green-500 rounded-full"></div>
    <div class="absolute top-2 left-1/2 w-2 h-2 bg-red-500 rounded-full"></div>
    <div class="absolute top-6 right-1/3 w-2 h-2 bg-blue-600 rounded-full"></div>
    <div class="absolute bottom-2 left-1/4 w-2 h-2 bg-gray-700 rounded-full"></div>
    <div class="absolute bottom-4 right-6 w-2 h-2 bg-green-500 rounded-full"></div>
  </div>
</header>

<!-- Menu Mobile (caché par défaut) -->
<nav id="mobile-menu" class="hidden md:hidden bg-white shadow-md absolute top-full left-0 w-full px-6 py-4 space-y-4">
  {navLinks.map(link => (
    <a href={link.href} class="block text-gray-800 font-medium hover:text-green-600 transition">
      {link.name}
    </a>
  ))}
  <a href="/image-209.png" class="block border-2 border-green-600 text-green-600 px-4 py-2 rounded-lg hover:bg-green-50 transition text-sm font-semibold">
    📄 Télécharger CV
  </a>
</nav>

<script>
  const menuBtn = document.getElementById('menu-btn');
  const mobileMenu = document.getElementById('mobile-menu');

  menuBtn.addEventListener('click', () => {
    mobileMenu.classList.toggle('hidden');
  });
</script>

